/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IndicationTypeConverterPipe } from './indication-type-converter.pipe';

describe('Pipe: IndicationTypeConvertere', () => {
  it('create an instance', () => {
    let pipe = new IndicationTypeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
